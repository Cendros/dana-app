export type StructureMinType = {
    structure: {
        id: number
        name: string
        image: string
    }
}

export type StructureType = {
    id: number
    name: string
    image: string
    address: string
    city: string
    postalCode: string
    longitude: number
    latitude: number
}